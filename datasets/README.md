### datasets

This module is to implement mechanism that imports and processes different datasets.

To use and existing dataset, import and use builder.build_dataset (read builder.py for further information)

To add a new dataset, add a new file corresponding to that dataset, and edit builder.py and reference cifar100.py for relevant function implementations.